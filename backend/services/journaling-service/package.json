{
  "name": "@unigarden/journaling-service",
  "version": "1.0.0",
  "private": true,
  "description": "UniGarden Journaling Service — encrypted permanent entries (PostgreSQL) + volatile drafts (Redis)",
  "scripts": {
    "dev": "ts-node src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "test:db": "ts-node src/test-db.ts",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev --name init"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "cors": "^2.8.6",
    "dotenv": "^16.4.5",
    "dotenv-expand": "^11.0.7",
    "express": "^4.21.1",
    "ioredis": "^5.4.1",
    "unigarden": "file:../../.."
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^4.17.21",
    "@types/node": "^22.9.0",
    "prisma": "^5.22.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
